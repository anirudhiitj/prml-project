# ── Core (required for all approaches) ──
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# ── PyTorch (CPU is sufficient for inference) ──
torch>=2.3.0
torchaudio>=2.3.0
soundfile>=0.12.1
numpy>=1.24.0
scipy>=1.11.0
matplotlib>=3.7.0
librosa>=0.10.0
accelerate
tqdm>=4.66.0
torchmetrics<=0.11.4
tensorboard
einops>=0.7.0
PyYAML
pandas
huggingface_hub

# ── Asteroid (Mayank's model — speech separation) ──
# NOTE: Do NOT install asteroid directly via pip on Windows — it pulls in
# pb_bss_eval and torch-stoi which require Microsoft C++ Build Tools.
# Instead install with:
#   pip install asteroid --no-deps
#   pip install asteroid-filterbanks julius pytorch-lightning "torch-optimizer<0.2.0" "torchmetrics<=0.11.4"
asteroid-filterbanks
julius
pytorch-lightning
torch-optimizer<0.2.0

# ── Anirudh cocktail_separation requirements ──
pyroomacoustics>=0.7.3
mir_eval>=0.7
pesq>=0.0.4
wandb>=0.16.0
hydra-core>=1.3.0
omegaconf>=2.3.0
